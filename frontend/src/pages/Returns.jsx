import './pages.css';

export default function Returns() {
  return (
    <div className="page container page-top">
      <p>Returns are accepted within 30 days for items in original condition.</p>
      <ul>
        <li>Items must be unworn, unwashed, and in original condition with tags attached.</li>
        <li>To start a return, email <a href="mailto:support@yourstore.com">support@yourstore.com</a> with your order number.</li>
        <li>Once we receive and inspect your return, we’ll refund the original payment method.</li>
      </ul>
      <p>Exchanges for a different size or color are processed once the return is received.</p>
    </div>
  );
}
